Status: Ready for Iris — 2026-06-12

# Brief: editorial line pass — B6 remainder + F9 on-ramp design

Context: Edith's reader's report (bugs.md B2–B6, features.md F9), RUBRIC.md
(audience fit 20% — her weakest-scoring dimension), the v1.3.5 floor-fix
(already landed; don't redo B1–B5).

1. **B6 remainder (manuscript edits, ch01–ch05 only):**
   - Rule 64 gauge and Rule 69 funnel diagrams: cut or make them earn the
     page (Edith: decoration). Your call which; one-line rationale each.
   - Name the hallucinated-*library*-API incident where the arriving reader
     looks (Rule 50 body or Ch2 search-first): the model inventing
     `requests.fetch_with_retry()` — frustration #1 deserves its name.
2. **F9 on-ramp design (proposal first, not edits):** the book hands the
   newbie "pre-commit hook," "push range," "deploy artifact" on page one.
   Propose the form — Chapter 0 primer vs. margin definitions on first use
   vs. glossary-first pointers — with the ten terms that must be taught,
   where each first appears, and a sample treatment of two. Deliver
   `plans/DESIGN_onramp.md`; Eddie picks the form before manuscript edits.
3. **Period/voice watch:** the floor-fix and F5 opener rewrites added
   sentences — sweep ch01–ch05 openers once for voice drift (plain, direct,
   first-person scars; no editorializing filler).

Constraints: one purpose per edit; rule statements don't change without a
bugs.md entry; build green after edits; 99-back-matter.md is Claude's this
wave — file back-matter findings, don't edit.
